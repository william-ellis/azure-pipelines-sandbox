trigger:
  - main

pool:
  vmImage: ubuntu-latest

steps:
  - pwsh: |
      Write-Host "##vso[task.setvariable variable=foo;isreadonly=True]foo"
      Write-Host "Foo: "
      Write-Host $Env:FOO
      Write-Host $(foo)
